#include "holberton.h"
/**
 * mul - to multiply two integers
 * @a: for intger a
 * @b: for integer b
 * Return: Always 0 succes
 */
int mul(int a, int b)
{
	if (a >= '0' && b <= 9)
		mul = a * b;
	return (0);
}

